#pragma once

enum class vk {
    backspace = VK_BACK,
    tab = VK_TAB,
    enter = VK_RETURN,
    shift = VK_SHIFT,
    ctrl = VK_CONTROL,
    alt = VK_MENU,
    caps_lock = VK_CAPITAL,
    esc = VK_ESCAPE,
    space = VK_SPACE,
    pg_up = VK_PRIOR,
    pg_dn = VK_NEXT,
    end = VK_END,
    home = VK_HOME,
    left = VK_LEFT,
    up = VK_UP,
    right = VK_RIGHT,
    down = VK_DOWN,
    print_scr = VK_SNAPSHOT,
    insert = VK_INSERT,
    del = VK_DELETE,
    key_0 = 0x30,
    key_1 = 0x31,
    key_2 = 0x32,
    key_3 = 0x33,
    key_4 = 0x34,
    key_5 = 0x35,
    key_6 = 0x36,
    key_7 = 0x37,
    key_8 = 0x38,
    key_9 = 0x39,
    key_a = 0x41,
    key_b = 0x42,
    key_c = 0x43,
    key_d = 0x44,
    key_e = 0x45,
    key_f = 0x46,
    key_g = 0x47,
    key_h = 0x48,
    key_i = 0x49,
    key_j = 0x4a,
    key_k = 0x4b,
    key_l = 0x4c,
    key_m = 0x4d,
    key_n = 0x4e,
    key_o = 0x4f,
    key_p = 0x50,
    key_q = 0x51,
    key_r = 0x52,
    key_s = 0x53,
    key_t = 0x54,
    key_u = 0x55,
    key_v = 0x56,
    key_w = 0x57,
    key_x = 0x58,
    key_y = 0x59,
    key_z = 0x5a,
    win_left = VK_LWIN,
    win_right = VK_RWIN,
    num_0 = VK_NUMPAD0,
    num_1 = VK_NUMPAD1,
    num_2 = VK_NUMPAD2,
    num_3 = VK_NUMPAD3,
    num_4 = VK_NUMPAD4,
    num_5 = VK_NUMPAD5,
    num_6 = VK_NUMPAD6,
    num_7 = VK_NUMPAD7,
    num_8 = VK_NUMPAD8,
    num_9 = VK_NUMPAD9,
    num_mult = VK_MULTIPLY,
    num_add = VK_ADD,
    num_sub = VK_SUBTRACT,
    num_dec = VK_DECIMAL,
    num_div = VK_DIVIDE,
    f1 = VK_F1,
    f2 = VK_F2,
    f3 = VK_F3,
    f4 = VK_F4,
    f5 = VK_F5,
    f6 = VK_F6,
    f7 = VK_F7,
    f8 = VK_F8,
    f9 = VK_F9,
    f10 = VK_F10,
    f11 = VK_F11,
    f12 = VK_F12,
    num_lock = VK_NUMLOCK,
    scroll_lock = VK_SCROLL,
    l_shift = VK_LSHIFT,
    r_shift = VK_RSHIFT,
    l_ctrl = VK_LCONTROL,
    r_ctrl = VK_RCONTROL,
    l_menu = VK_LMENU,
    r_menu = VK_RMENU,
    mouse_left = VK_LBUTTON,
    mouse_right = VK_RBUTTON,
    mouse_middle = VK_MBUTTON,
    mouse_x1 = VK_XBUTTON1,
    mouse_x2 = VK_XBUTTON2
};