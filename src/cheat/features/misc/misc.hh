#pragma once
#include "../../cheat.hh"

namespace features::misc {

	void spectators();
	void offscreen_esp();
	void auto_strafer(sdk::user_cmd*);
	void remove_flash();
	void remove_smoke();
	void world_modulation();
	void scope_lines();
	void glow_bloom( );
	void glow_stencil( );
	void thirdperson( );
}